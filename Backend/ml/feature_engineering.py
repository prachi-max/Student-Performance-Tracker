def process_student_data(student):

    return [
        student['attendance'],
        student['completed'],
        student['pending'],
        student['progress'],
        student['study_hours']
    ]